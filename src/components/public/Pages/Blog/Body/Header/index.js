import React from "react";
import HeaderItem from "./HeaderItem";

const Header = (props) => {
  return (
    <div className="header">
      <h1>Header</h1>
      <HeaderItem />
    </div>
  );
};

export default Header;
